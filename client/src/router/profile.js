// profile.js
import Profile from '../views/Profile.vue';

const profileRoutes = [
    {
        path: '/profile',
        name: 'Profile',
        component: Profile,
    }
];

export default profileRoutes;